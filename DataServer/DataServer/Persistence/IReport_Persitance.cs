using System.Collections.Generic;
using System.Threading.Tasks;
using DataServer.Models;

namespace DataServer.Persistence.Impl
{
    public interface IReport_Persitance
    {
        //REPORT CRUD
        //Task CreatePlaceReport(Report<Place> placeReport); in IPlacesReport_Persistence

        //Task CreateReviewReport(Report<Review> reviewReport); in IReviewReport_Persistence

        //Task CreateUserReport(Report<User> userReport); in IUserReport_Persistence

        //Task UpdatePlaceReport(Report<Place> placeReport); in IPlacesReport_Persistence

        //Task UpdateReviewReport(Report<Review> reviewReport); in IReviewReport_Persistence

        //Task UpdateUserReport(Report<User> userReport); in IUserReport_Persistence

        //Task<Dictionary<long, Report<Place>>> GetPlaceReports(); in IPlacesReport_Persistence

        //Task<Dictionary<long, Report<Review>>> GetReviewReports(); in IReviewReport_Persistence

        //Task<Dictionary<long, Report<User>>> GetUserReports(); in IUserReport_Persistence

        //Task DismissPlaceReport(long reportId); in IPlacesReport_Persistence

        //public Task DismissReviewReport(long reportId); in IReviewReport_Persistence

        //public Task DismissUserReport(long reportId); in IUserReport_Persistence
    }
}